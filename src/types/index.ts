export interface AboutCardProps {
  img: string;
  title: string;
  des: string;
  btn: string;
  btnClass?: string;
}

export interface AboutDataProps {
  img: string;
  title: string;
  des: string;
  btn: string;
  btnClass?: string;
}

export interface NavProps {
  to: string;
  name: string;
  more?: string;
}
